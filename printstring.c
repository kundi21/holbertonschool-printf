#include "printf.h"
/**
 * printstring - Print the content of a strin.
 * @format: Is a string
 */
void printstring(char *format)
{
	int n;

	for (n = 0; format[n] = '\0'; n++)
	{
		_putchar(format[n])
	}
}
