#include "printf.h" 
/**
 *
 *
 */
int _printf(const char *format, ...)
{
	char *buffer
	char *string

	for (i = 0; format[i] != '\0'; i++)
	{
		if (format == %)
		{ 
		string[i] = format[i] 
		string[i + 1] =	format [i + 1]
		}	 
